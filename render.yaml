services:
  - type: web
    name: meow-ai
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: MEOW_AI_API_KEY
        sync: false
      - key: MEOW_AI_API_URL
        sync: false
      - key: NODE_ENV
        value: production
